
export enum ImageRole {
  /**
   * The image is embedded in the active file
   */
  AsEmbed,
  /**
   * The image itself is the active file
   */
  AsActiveTab,
  /**
   * Unknown role
   */
  Unknown
}
